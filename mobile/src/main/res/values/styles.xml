<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Toolbar theme overlay to set the right font -->
    <style name="ToolbarThemeOverlay" parent="@style/ThemeOverlay.AppCompat.ActionBar"/>


    <style name="Button.Flat" parent="Widget.AppCompat.Button.Borderless.Colored" >
        <item name="android:textAllCaps">true</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:drawableTint">@color/colorAccent</item>
    </style>

    <style name="Button.Colored" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textAllCaps">true</item>
        <item name="colorButtonNormal">@color/colorAccent</item>
        <item name="android:drawableTint">@color/white</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
